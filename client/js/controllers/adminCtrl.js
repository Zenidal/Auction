var adminCtrl = angular.module('app.adminCtrl', ['ngCookies'])
        .controller('AdminCtrl', ['$scope', '$http', '$cookieStore', '$location',
            function ($scope, $http, $cookieStore, $location) {
            }]);